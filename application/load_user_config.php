<?php
/*
 * En este script el usuario realizara todas las configuraciones que crea necesaria para que su aplicacion funciones correctamente
 * Todo lo que se ubique aca se ejecutara despúes de cargar las librerias propias y externas y los modulos HTTP y Componente.
 * Y antes de ejecutar el requerimiento HTTP, digamos antes de que se ejecuten los filtros, controladores, component, cronController, etc. 
 */
